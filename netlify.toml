# Netlify 設定：Next.js の静的書き出し(out/)をそのまま配信する（APIなし・完全クライアント）。
# @netlify/plugin-nextjs は使わない（純静的のため publish=out で十分）。
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
